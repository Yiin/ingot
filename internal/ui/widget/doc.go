// Package widget holds the panel's smallest reusable pieces: the circular
// checkbox, the clamped note label, and the note card (Row) that
// assembles them into the app's most-seen surface.
package widget
